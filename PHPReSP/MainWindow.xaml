<Window x:Class="PHPReSP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PHPReSP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>

        </Grid.RowDefinitions>


        <Button Content="Import CSV" HorizontalAlignment="Left" Margin="715,386,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.647,0.549" Click="Button_Click"/>
        <ListView x:Name="ListViewSales" Margin="238,10,10,71">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Sale ID" Width="100" DisplayMemberBinding="{Binding Path=SaleID}"/>
                    <GridViewColumn Header="Product" Width="150" DisplayMemberBinding="{Binding Path=Product}"/>
                    <GridViewColumn Header="Cost" Width="40" DisplayMemberBinding="{Binding Path=Cost}"/>
                    <GridViewColumn Header="Number of Items" Width="200" DisplayMemberBinding="{Binding Path=ItemsInsale}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
